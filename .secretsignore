# Ignore false positives for password-related code
# These are function parameters and environment variable fallbacks, not hardcoded secrets

# Function parameters named 'password' are not secrets
password
defaultPassword
passwordHash
password_hash

# Environment variable fallbacks
ADMIN_DEFAULT_PASSWORD
